input {
  tcp {
    port => 5000
    codec => json_lines
  }
}

filter {
  if ![timestamp] {
    mutate { add_field => { "received_at" => "%{@timestamp}" } }
  }

  if [event_type] == "ANOMALY" {
    mutate {
      convert => {
        "anomaly_deviation" => "float"
        "anomaly_actual" => "float"
        "anomaly_predicted" => "float"
      }
    }
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "energy-server-logs-%{+YYYY.MM.dd}"
  }

  if "DB_STRESS" in [message] {
    http {
      url => "https://api.telegram.org/bot8215343257:AAEzjVQqbNVJTpsgUpubQcqfRP9amJn_7Vs/sendMessage"
      http_method => "post"
      content_type => "application/json"
      format => "message"
      message => '{"chat_id": "-5204085463", "parse_mode": "Markdown", "text": "🚨 *ENERGY SYSTEM ALERT* 🚨\n\n⚠️ *Status:* Database Unreachable\n📉 *Action:* Data rerouted to RabbitMQ (Fallback Enabled)\n\n📝 *Error Details:* %{message}"}'
    }
  }

  if "ANOMALY_ALERT" in [message] {
    http {
      url => "https://api.telegram.org/bot8215343257:AAEzjVQqbNVJTpsgUpubQcqfRP9amJn_7Vs/sendMessage"
      http_method => "post"
      content_type => "application/json"
      format => "message"
      message => '{"chat_id": "-5204085463", "parse_mode": "Markdown", "text": "🤖 *AI ANOMALY DETECTED* 🤖\n\n📊 *Deviation:* %{anomaly_deviation}%\n🔮 *Predicted:* %{anomaly_predicted} kW\n⚡ *Actual:* %{anomaly_actual} kW\n\n_The AI Model has detected an unusual consumption pattern._"}'
    }
  }

  # --- DEBUG CONSOLE ---
  stdout { codec => rubydebug }
}